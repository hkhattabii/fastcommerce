const {
  USR,
  PWD_REQ,
  GNDR,
  BRND,
  CTGR,
  PRDT,
  IMG,
  DISC,
  CRT_ROW
} = require('./strings');

const TABLES = [USR, PWD_REQ, GNDR, BRND, CTGR, PRDT, IMG, DISC, CRT_ROW];

module.exports = TABLES;
