const {
  USR,
  PWD_REQ,
  GNDR,
  BRND,
  CTGR,
  PRDT,
  IMG,
  DISC,
  CRT_ROW,
  ADDRESS,
  BILL,
  BILLPRDT,
  DELIVERY
} = require('./strings');

const TABLES = [USR, PWD_REQ, GNDR, BRND, CTGR, PRDT, IMG, DISC, CRT_ROW, ADDRESS, BILL, BILLPRDT, DELIVERY];

module.exports = TABLES;
